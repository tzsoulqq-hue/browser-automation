# 安全政策

请不要在 issue、日志、截图或调试产物中提交真实凭据、token、cookie、验证码、代理凭据、storage state 或可复用会话材料。

本仓处理的浏览器会话天然属于敏感资产：

- 公共和共享私有契约只能传递 `secret_ref`、`artifact_ref` 或最小必要元数据；
- 日志不得输出原始 cookie、OTP、账号密码、代理凭据或完整会话内容；
- 截图、trace、HAR、录像和下载文件如可能含敏感信息，必须放在忽略路径并按最小保留原则处理。
